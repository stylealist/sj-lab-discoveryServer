server:
  port: 8761
  servlet:
    context-path: /

spring:
  application:
    name: discoveryservice
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
